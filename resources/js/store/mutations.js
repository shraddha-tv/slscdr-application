// set top message popup to array
export const set_message = (state, message) => {
    return state.message = message
}

// set loader value
export const set_loader = (state) => {
    return state.loader = !state.loader
}

